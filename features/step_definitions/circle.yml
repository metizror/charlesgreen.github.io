## Customize the test machine
machine:
  ruby:
    version: 2.3.0

## Customize dependencies
dependencies:
  pre:
    - gem install bundler
    - gem install jekyll
    - npm install chimp
    - npm install jshint

